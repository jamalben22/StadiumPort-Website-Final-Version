import React from 'react';

// SEO component is not used in App Router as metadata is handled by page.tsx
// This is a placeholder to prevent build errors during migration.
// Future TODO: Move dynamic metadata logic to page.tsx generateMetadata
export function SEO(props: any) {
  return null;
}
